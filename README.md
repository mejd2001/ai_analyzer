# AI Sales Analyzer
A Python tool to analyze sales data from Excel, generate insights, detect anomalies, predict sales, and create reports/dashboards.

## Setup
pip install -r requirements.txt

## Usage
streamlit run app.py